repos:
  - repo: local
    hooks:
      - id: cargo-lint
        name: cargo lint (fmt + clippy)
        entry: bash -c 'just lint'
        language: system
        pass_filenames: false
        types_or: [rust, toml]
